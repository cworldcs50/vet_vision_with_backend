import 'package:flutter/material.dart';

class TokenPassword extends StatelessWidget {
  const TokenPassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
